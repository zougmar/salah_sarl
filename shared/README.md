# Shared Resources

This directory is reserved for future shared assets (types, utilities, documentation) that can be consumed by both the backend and frontend of SalahElecSarl. For now it simply acts as a placeholder so the workspace structure matches the architectural blueprint described in the project brief.

